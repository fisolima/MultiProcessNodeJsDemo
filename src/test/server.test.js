const chain = require('chai');
const should = require('should');
const sinon = require('sinon');

describe('Server Test Unit', () => {
    it('Should succeed', () => {
        should.ok('Succeeded');
    });
});